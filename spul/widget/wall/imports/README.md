# wall/imports

Copies of imported checkpoint JSONL land here (e.g. from `mac/import-ef-ee-checkpoint.sh`).  
Do not commit secrets. EF_EE source lives on Bill’s Mac iCloud path, not in the cloud VM.
