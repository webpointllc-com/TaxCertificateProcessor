# Environment

No secrets required for the minimal registry proto.

```bash
PORT=3000
NODE_ENV=production
```

Optional future keys (dashboard only, never commit):

```bash
# SESSION_SECRET=
# FEEDBACK_WEBHOOK=
```
