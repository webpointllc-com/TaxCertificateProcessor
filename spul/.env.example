# Environment

No secrets required for the minimal registry proto.

```bash
PORT=3000
NODE_ENV=production
```

# Optional (dashboard only — never commit real values)
# GROQ_API_KEY=          # free Groq proto narration; URLs still hard-locked from index
# SESSION_SECRET=
# FEEDBACK_WEBHOOK=
