# Environment (no secrets)

Copy to `.env` locally or set on the AWS host. Never commit real values.

```bash
PORT=3000
NODE_ENV=production
HOST=0.0.0.0
```

# Optional — set only in host/SSM/dashboard, never in git
# GROQ_API_KEY=
# SESSION_SECRET=
# FEEDBACK_WEBHOOK=
